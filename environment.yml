name: quantum-nonogram
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - ipykernel
  - jupyterlab
  - pip
  - pip:
    # the package pulls its own dependencies from pyproject.toml; listing them
    # again here only lets the two drift apart
    - -e ".[dev]"
